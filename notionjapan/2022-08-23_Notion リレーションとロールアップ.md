# Notion | リレーションとロールアップ

| 項目 | 内容 |
|------|------|
| **動画URL** | https://www.youtube.com/watch?v=N4x0eKgjIl8 |
| **チャンネル** | Notion Japan  |
| **公開日** | 2022年08月23日 |
| **再生時間** | 3:49 |
| **再生回数** | 914 |

## 概要
データベースを接続して活用できるようになると、さらに強力なツールになります。リレーションを使って別のデータベースのデータに関連付け、ロールアップ機能で紐付いたデータの別のプロパティを表示する方法を紹介します。

Notionへのサインアップはこちらからhttps://bit.ly/3aBOpoJ

🌎Notionについて詳しく知る
Notion Japan on Twitter: https://twitter.com/NotionJP
Notion Community in Japan:  https://www.notion.so/notion/Notion-Community-in-Japan-1ee5a66e6d3e44d09a1e6fdf82b5f9a5

---

## 文字起こし

Notionで複数のデータベースを使用していますか？
もしそうなら、データベース
間でアイテムを接続したい時が来るかもしれません。
これは
リレーションとロールアップで実現できます。この
ビデオでは、あるデータベースのページを別のデータベースのページに接続する方法（リレーション）を紹介します。
また、
関連するデータベースから
別のデータベースに特定のプロパティをプルする方法も学びます。この
機能はロールアップと呼ばれます。
架空のエンジニアリングチーム向けに設計されたワークスペースを見てみましょう。
プロジェクトとタスクという2つのデータベースがあります。この
場合、テストはプロジェクトを構成するアクションな
ので、
すべてのタスクを対応するプロジェクトに接続したいと考えるのは当然です。
リレーションを使用してこれを実現します。
タスクデータベースを見てみましょう。
このテーブルにはいくつかのプロパティがあります。
最初のステップは、リレーションプロパティを追加することです。
テーブルの右側にあるプラス記号をクリックします。
新しいプロパティに名前を付けます。ここでは
プロジェクトとします。リレーションは詳細設定
セクションの下部にあります。ここをクリックします。
テーブルをリンクするデータベースを選択するように求められます。ここをクリックします。
次に、
検索バーでそのデータベースを見つけるか、ドロップダウンをスクロールして
データベースを選択し、「
リレーションの作成」をクリックします。 テーブルに新しいリレーションプロパティが
表示されます。
次に、
テストが関連付けられているプロジェクトを指定します。
たとえば、クエリキャッシュロジックの書き換えは
パフォーマンス改善プロジェクトに対応します。
両方のページを接続するには、
新しいプロジェクトセル内をクリックして「パフォーマンス改善」を選択します。これで、
プロジェクトページを表示できます。ここで
名前をクリックしてアクセスできます。
他のデータベースに移動して探す必要はありません。2つの
データベースを相互にリンクするたびに、
変更は両方の
データベースに表示されます。プロジェクトのデータベースに新しい
リレーション列が自動的に追加されていることがわかります。ここで、
先ほどリンクしたタスクページも見つけることができます。ここをクリックすると、
タスクページに移動します。
必要に応じて、新しい列の名前を変更します。
最後に、
残りのタスクをプロジェクトにすばやくリンクしましょう。これで完了です。
すべてのプロジェクトが
対応するタスクにリンクされ、すべてのタスクが対応する
プロジェクトにリンクされました。
これがリレーションの概要です。次に、
ロールアップで何ができるかをお見せします。前述したように、この機能は
1つのデータベースから
別のデータベースにデータをプルします。タスクデータベースでは、
各タスクにかかる推定開発時間を指定しました。
プロジェクト全体の所要時間を知りたい場合はどうすればよいでしょうか。
プロジェクト データベースに移動して、
テーブルにロールアップ プロパティを追加する必要があります。
テーブルの右側にあるプラス記号をクリックして、プロパティに名前を付けます。
詳細セクションに移動し、今回はロールアップを選択します。
新しいロールアップ プロパティが表示されます。それを
クリックして、ロールアップの構成を選択します。
これは、
データを取得するデータベースと表示する特定の
プロパティを指定する部分です。ここをクリックして
タスク データベースを選択し、
ここをクリックしてタスク
データベースから
関心のあるプロパティを選択します。
計算セクションで合計を選択します。これにより、このプロジェクトに必要な Def 時間の合計が
自動的に計算されます。これで、プロジェクト全体の時間の
見積もりが
プロジェクトのデータベースの列としてきれいに表示されます。これは、
リレーションとロールアップを使用して作成できるすべての便利な接続のプレビューでした。
ワークスペースが大きくなるにつれて、
このようにデータベースをリンクする機会が増え、結果として、
手元にあるデータをよりよく理解できるようになります。
[音楽]
